# 05 — Primitives: Button, inputs, Card, Badge, Alert…

**What to build:** Replace the shared visual pieces with shadcn components themed by the tokens: Button (instead of `.button primary/secondary`), Input, Label/Field, Select, Checkbox, Card, Badge, Alert (notices), Empty, Separator.

**Blocked by:** 04

**Status:** needs-triage

**Effort:** M–L

Needs a short design pass after 04: which variants to add so buttons, notices and badges keep their current character.
