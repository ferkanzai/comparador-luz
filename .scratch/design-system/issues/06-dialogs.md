# 06 — Dialogs on shadcn

**What to build:** Move `Modal` and `ConfirmDialog` from the native `<dialog>` to shadcn's `Dialog` and `AlertDialog`.

**Blocked by:** 05

**Status:** needs-triage

**Effort:** M

Needs triage: keyboard focus, scroll locking and phone full-screen behaviour are covered by the browser tests and must keep passing. Consider codebase-review ticket 26 (stacked dialogs) at the same time.
