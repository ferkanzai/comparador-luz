/* The page shell. The header's inner row shares the main column's width. */

export const shell =
  "mx-auto max-w-[1280px] px-10 max-[1000px]:px-6 max-[520px]:px-4 min-[1400px]:px-8";
/* Hidden above the page until it receives focus. */
export const skipLink =
  "absolute -top-[100px] left-5 z-20 rounded-md bg-foreground p-3 text-card focus:top-2.5 focus-visible:rounded-sm";
